<%= form_with(model: route) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
        <% route.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 

  <div class="field">
    <%= form.label :start_at %>
    <%= form.datetime_select :start_at %>
  </div>

  <div class="field">
    <%= form.label :ends_at %>
    <%= form.datetime_select :ends_at %>
  </div>

  <div class="field">
    <%= form.label :travel_time %>
    <%= form.time_select :travel_time %>
  </div>

  <div class="field">
    <%= form.label :total_stops %>
    <%= form.number_field :total_stops %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select :status, Route.statuses.keys, prompt: "seleciona una accion" %>  
     
  </div>
  
   <%= form.fields_for :organizations do |organization|%>
    <div class="form-group">
      <%= organization.select :name, id:'tipo' , Organization.all, class:"form-control" %>
    </div>
  <%end%>

    <div class="opciones">
      <div><form>Nombre: <input name="op1" required="required" size="30" type="text" />

      Direccion: <input name="op1" size="30" type="text" />

      DNI: <input name="op1" size="30" type="text" />

      <input class="btn" name="submit" type="submit" value="ENVIAR" /></form></div>
      <div id="op2"><form>Razón Social: <input name="op1" required="required" size="30" type="text" />

      Direccion: <input name="op1" required="required" size="30" type="text" />

      RUC: <input name="op1" size="30" type="text" />

      <input class="btn" name="submit" type="submit" value="ENVIAR" /></form></div>
    </div>
  
  <%= form.fields_for :drivers do |driver|%>
    <div class="form-group">
      <%= driver.select :name, Driver.all, class:"form-control" %>
    </div>
  <%end%>

  <%= form.fields_for :vehicles do |vehicle|%>
    <div class="form-group">
      <%= vehicle.select :plate ,Vehicle.all, class:"form-control" %>
    </div>
  <%end%>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<script>
    
  function elegir_opcion(combo) {
  $tipo = jQuery(combo).val();
  $campos = jQuery("#"+$tipo).html();
  jQuery(".opcionesWrapper").html($campos);
  }
</script>